# https-github.com-ise-uiuc-TitanFuzz

此仓库原本为空。为了能立即运行，我添加了一个最小的 Python Flask 示例应用。

快速运行步骤：

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/python3 app.py
```

访问 http://127.0.0.1:8000 查看响应。

如果你希望我改用其他语言/框架（例如 Node.js、Go、Rust），告诉我即可。